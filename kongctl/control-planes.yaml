_defaults:
  kongctl:
    namespace: tanuki-pii

control_planes:
  - ref: tanuki-pii
    name: tanuki-pii
    description: Kong Gateway for Jungle Store.
    auth_type: pki_client_certs
    cluster_type: CLUSTER_TYPE_CONTROL_PLANE
    labels:
      environment: local
